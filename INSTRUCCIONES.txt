========================================
  CONTENTFORGE - PLAN DEL PROYECTO
========================================

OBJETIVO
--------
App web local (localhost) que genera imágenes estáticas
para TikTok (carruseles, frases, anuncios) usando
Python + Flask + Pillow.

ESTRUCTURA DEL PROYECTO
-----------------------
contentforge/
│
├── app.py                  # Punto de entrada - servidor Flask + rutas
├── config.py               # Configuración global (colores, fuentes, medidas)
├── requirements.txt        # Dependencias (Flask, Pillow)
│
├── fonts/                  # Fuentes .ttf para los textos ✅
│   ├── Micro5-Regular.ttf
│   ├── RubikGlitch-Regular.ttf
│   └── UncialAntiqua-Regular.ttf
│
├── images/                 # Sets de imágenes para fondos y overlays
│   ├── backgrounds/
│   │   ├── preinstaladas/   # Fondos descargados (dots, hearts, spots, stripes, zigzag)
│   │   └── subidas/         # Fondos subidos por el usuario vía navegador
│   └── overlays/
│       ├── placeholder/     # Generado por código (círculo, estrella, cuadrado)
│       └── (packs reales aquí)
│
├── generator/              # Módulo de generación de imágenes
│   ├── __init__.py
│   ├── base.py             # Motor de dibujo (PlantillaBase)
│   └── carousel.py         # Clase Carrusel: procesa texto + dibuja + guarda
│
├── utils/                  # Utilidades reutilizables
│   ├── __init__.py
│   ├── colors.py           # Paletas de colores predefinidas
│   ├── fonts.py            # Escáner y selector de fuentes ✅
│   ├── images.py           # Gestión de imágenes de fondo ✅
│   ├── layouts.py          # LayoutEngine: plantillas imagen+texto ✅
│   └── typography.py       # Manejo de fuentes y textos
│
├── templates/              # HTML (Flask los usa para la web)
│   └── index.html          # Formulario web para generar carruseles ✅
│
├── static/                 # CSS/JS para la interfaz web
│   ├── style.css           # Estilos del formulario ✅
│   └── script.js           # Sincronización paleta ↔ color picker ✅
│
├── tests/                  # Tests unitarios (pytest) ✅
│   ├── test_base.py        ✅
│   ├── test_colors.py      ✅
│   ├── test_carousel.py    ✅
│   ├── test_fonts.py       ✅
│   ├── test_images.py      ✅
│   └── test_layouts.py     ✅
│
└── output/                 # Ya no se usa — los slides se descargan como ZIP


PRINCIPIO MODULAR
-----------------
- Cada archivo tiene UNA sola responsabilidad.
- PlantillaBase = motor de dibujo puro. Crea imagen + dibuja texto con
  fuente, tamaño y alineación configurables. No sabe de archivos ni carpetas.
  Método público: crear_imagen(texto, fuente, tamano, alineacion,
  contorno, sombra) → retorna PIL.Image
- Carrusel = clase principal. Hereda de PlantillaBase.
  __init__ procesa texto crudo → lista de slides.
  generar() crea carpeta, llama crear_imagen() por cada slide, guarda.
  Recibe y pasa parámetros de fuente, tamaño, alineación, contorno, sombra.
- Carrusel maneja N slides (1-20), cubre casos de quote/anuncio.
- FontManager = escanea fonts/ y provee lista de fuentes disponibles. ✅
- ImageManager = administra imágenes en images/. ✅
  Métodos: listar_imagenes(), obtener_ruta(), cargar_fondo()
- LayoutEngine = aplica plantillas predefinidas (texto+imágenes).
  Método público: aplicar(texto, imagenes, template) → dict de posiciones
- VisualEditor (v1.4) = canvas JS en frontend para edición en vivo.
- Output: cada generación descarga un ZIP con los slides vía el navegador (diálogo Guardar como).
  No hay carpeta fija — el usuario elige dónde guardar al descargar.
- Preview vía AJAX: ruta /preview devuelve la imagen renderizada en memoria
  (sin guardar) para mostrarla en el navegador antes de generar el carrusel.


ROADMAP DE VERSIONES
--------------------
v1.0 (Texto avanzado)                           ✅ COMPLETADA
  - Fuentes externas + selector
  - Tamaño de fuente manual
  - Alineación (izquierda, centro, derecha, justificado)
  - Presets de tamaño de slide (cuadrado, vertical, horizontal)
  - Cantidad de slides manual

v1.1 (Mejoras tipográficas + preview)            ✅ COMPLETADA (Días 7 ✅, 8 ✅, 9 ✅)
  - Bordes y sombreado en texto                          ✅
  - Selector de color interactivo (color picker hex)     ✅
  - Preview vía AJAX: renderizar y mostrar imagen        ✅
    en el navegador sin recargar ni guardar
  - Alineación justificado (espaciado entre palabras)    ✅

v1.2 (Imagen de fondo + descarga ZIP)              ✅ COMPLETADA (Días 10a ✅, 10b ✅, 10c ✅, 10d ✅, 10e ✅)
  - Fondo del slide: imagen (carpeta local + upload)     ✅ DÍA 10a-10b
  - Selector y preview de imágenes de fondo               ✅ DÍA 10b
  - Opacidad de imagen para legibilidad del texto         ✅ DÍA 10c
  - Upload de imágenes desde el navegador                 ✅ DÍA 10d
  - Output como ZIP descargable (sin carpeta fija)        ✅ DÍA 10e
  - Preview actualizado con el fondo                      ✅ DÍA 10b

v1.3 (Imágenes encima + layouts)                 ⬅️ EN PROGRESO (Día 11)
  - Overlay decorativo con placeholder              ✅ DÍA 11a
  - Selector de posición y tamaño del overlay       ✅ DÍA 11a
  - Show/hide en Borde y Sombra del texto           ✅ DÍA 11a
  - Rotación, flip, opacidad, borde, sombra overlay ✅ DÍA 11b
  - Modo collage (N imágenes)                       ⬅️ DÍA 11c
  - Modo tapiz (patrón repetitivo)                  ⬅️ DÍA 11d
  - Tests finales + packs HeritageType              ⬅️ DÍA 11e

v1.4 (Editor visual completo)                    🔮 FUTURO
  - Canvas en vivo en el navegador
  - Capas, arrastrar, soltar, redimensionar
  - Preview en tiempo real (sin llamada al servidor)


DÍA 1: Setup + Flask "Hola Mundo" ✅ COMPLETADO
  - Verificar Python instalado ✅
  - Crear entorno virtual (.venv) ✅
  - pip install flask pillow ✅
  - Escribir app.py con ruta "/" que diga "Hola Mundo" ✅
  - Ejecutar y ver en navegador (localhost:5000) ✅

DÍA 2: Primera imagen con Pillow ✅ COMPLETADO
  - Crear generator/base.py (clase PlantillaBase) ✅
  - Crear utils/colors.py (paletas de colores) ✅
  - Crear utils/typography.py (manejo de fuentes) ✅
  - Generar imagen con fondo + texto desde app.py ✅

DÍA 3: Carrusel funcional ✅ COMPLETADO
  - base.py refactorizado: PlantillaBase es solo motor de dibujo
    - __init__ solo recibe color_fondo, color_texto (sin self.texto)
    - _crear_imagen(texto) dibuja y retorna PIL.Image
    - generar() eliminado
  - carousel.py completo:
    - _dividir_oraciones: split texto por . ! ?
    - _distribuir: reparte N elementos en M grupos parejos
    - _dividir_parrafos: asigna slides proporcionalmente entre párrafos
    - _procesar_textos: texto crudo → lista de slides
    - generar(): crea carpeta timestamp, itera slides, dibuja con _crear_imagen(), guarda
  - app.py conectado con ruta /generar usando Carrusel (texto de prueba)
  - Output: 1 carpeta timestamp por generación en output/

DÍA 4: Interfaz web (formularios) ✅ COMPLETADO
  - app.py: rutas `/` (GET con index.html) y `/generar` (POST con formulario) ✅
  - templates/index.html: form con textarea + selector de paletas + botón ✅
  - static/style.css: estilos básicos (tarjeta centrada, formulario, botón) ✅
  - Ctrl+F5 para refrescar caché del navegador ✅
  - Nota: Se eliminaron quote.py y announcement.py (Carrusel los cubre) ✅

DÍA 5: Texto avanzado — Fuentes y tipografía (v1.0) ✅ COMPLETADO
  - Crear carpeta fonts/ con .ttf (Micro5, RubikGlitch, UncialAntiqua) ✅
  - utils/fonts.py: FontManager escanea fonts/, carga fuentes disponibles ✅
  - base.py: acepta fuente personalizada + tamaño manual + alineación ✅
  - PlantillaBase._crear_imagen(): nuevos parámetros (fuente, tamaño, alineación) ✅
  - Auto-ajuste: si el texto no entra, reduce tamaño hasta que quepa ✅
  - index.html: selector de fuente, slider de tamaño, radios de alineación ✅
  - app.py: lee nuevos campos y los pasa al Carrusel ✅
  - tests/test_fonts.py: tests unitarios para FontManager ✅

DÍA 6: Presets de tamaño y slides manuales (v1.0) ✅ COMPLETADO
  - config.py: agregar PRESETS_TAMANO (1080×1080, 1080×1920, 1920×1080) ✅
  - carousel.py: recibir num_slides y tamaño desde el formulario ✅
  - index.html: selector de preset de tamaño, input de cantidad de slides ✅
  - CSS responsivo para los nuevos controles ✅

DÍA 7: Bordes y sombreado en texto (v1.1) ✅ COMPLETADO
  - base.py: agregar parámetros contorno (color/grosor) y sombra (offset/color) a crear_imagen ✅
  - Dibujar sombra primero (desplazada), luego contorno (texto outline), luego texto ✅
  - carousel.py: pasar nuevos parámetros en __init__ y generar() ✅
  - app.py: función hex_a_rgb() para convertir hex a tupla RGB ✅
  - app.py: leer borde_activar, borde_color, borde_grosor, sombra_activar,
    sombra_offset_x, sombra_offset_y, sombra_color y pasarlos a Carrusel ✅
  - index.html: reorganizar formulario en 6 fieldset (Texto, Tipografía, Color,
    Borde, Sombra, Slide) con checkbox show/hide vía CSS ✅
  - index.html: controles de borde (color picker + range grosor) y sombra
    (color picker + range offset X/Y) ✅
  - Tests: test_base.py actualizado para contorno y sombra ✅
  - Fix: test_fonts.py — assert len(fuentes) == 3 → len(fuentes) > 0 ✅

DÍA 8: Selector de color interactivo (v1.1) ✅ COMPLETADO
  - colors.py: valores RGB → hex para compatibilidad con el navegador ✅
  - index.html: color pickers (type="color") para fondo y texto, más el select de
    paletas predefinidas que sincronizan los pickers vía data-* attributes ✅
  - app.py: recibe hex del form, prioriza color pickers, fallback a paleta ✅
  - static/script.js: sincronización paleta ↔ color picker en tiempo real ✅
  - static/style.css: estilos para inputs color ✅
  - Tests: test_colors.py actualizado con valores hex ✅

DÍA 9: Preview vía AJAX — Layout y preparación (v1.1) ✅ COMPLETADO
  - index.html: split layout con formulario a la izquierda y preview a la derecha ✅
  - index.html: grids compactos (grid-2, grid-3) dentro de fieldsets ✅
  - index.html: alineación "Justificado" agregada al radio group ✅
  - index.html: área de preview (img) + navegación ◀ ▶ en columna derecha ✅
  - index.html: botón "Generar Carrusel" fuera del form, en columna derecha ✅
  - static/style.css: layout 2 columnas (flex), 500px form-col, preview-col flex 1 ✅
  - static/style.css: grids, radio-group, fieldset compacto, labels reducidos ✅
  - static/style.css: preview-container, nav-controls, btn-generar ✅
  - app.py: ruta /preview (POST, renderiza 1 slide, devuelve PNG) ✅
  - carousel.py: método render_slide() para preview individual ✅
  - script.js: AJAX con fetch, debounce 300ms, navegación entre slides ✅
  - script.js: header X-Total-Slides para contador correcto de slides ✅
  
DÍA 10a (v1.2): Imagen de fondo — Fase 1: Carpeta local ✅ COMPLETADO
  - utils/images.py: ImageManager — escanea images/ y provee lista de imágenes
    disponibles, carga y redimensiona imágenes para usarlas como fondo ✅
  - tests/test_images.py: tests unitarios para ImageManager ✅
  - config.py: agregar RUTA_IMAGENES apuntando a la carpeta images/ ✅

DÍA 10b (v1.2): Imagen de fondo — Fase 2: Integración en base + frontend ✅ COMPLETADO
  - base.py: _crear_imagen acepta ruta_imagen_fondo opcional; si se provee,
    abre, convierte a RGB y redimensiona como capa base ✅
  - carousel.py: ruta_imagen_fondo en __init__, render_slide() y generar() ✅
  - app.py: importa img_manager, lee imagen_fondo del form en /generar
    y /preview, pasa ruta resuelta al Carrusel ✅
  - index.html: fieldset Fondo con select de imágenes disponibles ✅
  - Preview AJAX actualizado automáticamente (FormData) ✅
  - Tests: test_base.py y test_carousel.py actualizados con 3 tests nuevos ✅

DÍA 10c (v1.2): Opacidad de imagen de fondo ✅ COMPLETADO
  - base.py: parámetro opacidad_imagen (float 0.0-1.0) en _crear_imagen().
    Si opacidad < 1.0, usar Image.blend(imagen_fondo, color_solido, opacidad)
    para mezclar antes de dibujar texto ✅
  - carousel.py: pasar opacidad_imagen en __init__, render_slide() y generar() ✅
  - app.py: leer opacidad_imagen del form (0-100), convertir a float 0.0-1.0 ✅
  - index.html: slider range 0-100 en fieldset Fondo con label "Opacidad" ✅
  - script.js: indicador de porcentaje en vivo al mover el slider ✅
  - Preview AJAX captura automáticamente el nuevo campo ✅

DÍA 10d (v1.2): Imagen de fondo — Fase 3: Upload de imágenes ✅ COMPLETADO
  - utils/images.py: escaneo por origen (preinstaladas / subidas),
    método guardar_archivo() con sanitización + timestamp ✅
  - images/uploads/: subcarpeta para imágenes subidas ✅
  - app.py: ruta /upload-fondo (POST) + ruta /listar-imagenes (GET) JSON ✅
  - index.html: select con optgroups "Preinstaladas" / "Subidas",
    input file + botón "Subir" + feedback visual ✅
  - static/script.js: upload vía fetch, recargarSelectFondo() sin recargar ✅
  - tests: 3 tests nuevos (agrupadas, upload válido, extensión inválida) ✅

DÍA 10e (v1.2): Output como ZIP descargable ✅ COMPLETADO
  - generator/carousel.py: render_all() devuelve lista de PIL Images sin guardar ✅
  - app.py: /generar crea ZIP en memoria con zipfile + BytesIO,
    devuelve send_file con Content-Disposition: attachment ✅
  - templates/index.html: eliminado campo ruta_destino, eliminado resultado,
    agregado loader "Generando..." ✅
  - static/script.js: click en "Generar Carrusel" hace fetch, recibe blob,
    crea link de descarga → diálogo Guardar como nativo ✅
  - El usuario elige carpeta al descargar (no hay carpeta output/ fija) ✅

DÍA 11a (v1.3): Modo "imagen" — visible en navegador ✅ COMPLETADO
  - Crear utils/layouts.py: LayoutEngine con:
    - Placeholders decorativos: círculo, estrella, cuadrado
      (generados con PIL, con transparencia, sin archivos externos)
    - calcular_colocacion(modo, params, ancho, alto) → dict
      Soporta: modo "imagen" (posicion: izq/der/arriba/abajo/centro)
      y modo "detras" (imagen decorativa de fondo).
      Tamaño configurable 50-200%, permite desborde del canvas.                          ✅
  - Modificar base.py: _crear_imagen() acepta colocaciones (lista de dicts).
    Nuevo método _componer_imagenes(fondo, colocaciones):
    - Convierte fondo a RGBA → pega cada overlay redimensionado
      respetando transparencia (Image.alpha_composite)
    - Llamado después del fondo, antes del texto                                           ✅
  - Modificar carousel.py: acepta colocaciones en __init__ y las pasa
    a render_slide() / render_all() / generar()                                            ✅
  - Modificar utils/images.py: escaneo de packs en images/overlays/
    Método listar_overlays_agrupadas() → {"placeholder": [formas], ...}                    ✅
  - Modificar app.py:
    - En / pasar overlays_agrupados al template
    - En /generar y /preview: leer overlay_activo, overlay_pack,
      overlay_archivo, overlay_posicion, overlay_tamano
    - Si overlay_activo: resolver imagen (placeholder o archivo),
      calcular colocación, pasar al Carrusel                                               ✅
  - Modificar index.html:
    - Fieldset "🖼️ Imagen Overlay" con checkbox, select de packs,
      radio de posición, slider de tamaño                                                  ✅
    - Borde, Sombra y Fondo del texto: controles ocultos por defecto,
      se muestran al checkear "Activar" (show/hide)                                        ✅
  - Modificar script.js: mostrar/ocultar controles según checkbox,
    cambiar opciones al seleccionar pack, actualizar preview.
    Función genérica initToggle() para show/hide de Borde, Sombra y Fondo                  ✅
  - Modificar style.css: estilos para el nuevo fieldset                                    ✅
  - Tests: test_layouts.py (13 tests: placeholders, calcular_colocacion)                   ✅
  - Tests: test_base.py (3 tests nuevos: colocaciones se pegan en posición correcta)       ✅
  - RESULTADO FINAL: formulario web con controles de overlay,
    preview en vivo con imagen overlay visible sobre el fondo.
    Borde, Sombra y Fondo ahora con show/hide vía checkbox "Activar".                      ✅

DÍA 11b (v1.3): Modo "imagen" completo — rotación, flip, opacidad, borde, sombra ✅ COMPLETADO
  - base.py: _componer_imagenes ahora aplica por cada colocación:
    - Flip X/Y con .transpose (antes de rotación)
    - Rotación con .rotate(expand=True) manteniendo centro
    - Opacidad individual modificando canal alpha
    - Borde por outline sobre alpha (MaxFilter + ImageChops.subtract,
      sigue la silueta de la forma, no un rectángulo)
    - Sombra con GaussianBlur(radius=4) + 60% opacidad para efecto realista
  - utils/layouts.py: FORMAS_PLACEHOLDER = ["circulo", "estrella", "cuadrado"]
    - Color fijo por defecto (azul claro) o configurable por el usuario
    - Alpha 255 (totalmente opaco, sin transparencia parasitaria)
    - Se eliminaron "diamante" y "marco" (redundantes con rotación y opacidad)
  - app.py: lee overlay_rotacion, overlay_flip_x/y, overlay_opacidad,
    overlay_borde_activar/color/grosor, overlay_sombra_activar/offset/color,
    overlay_color_ph (color personalizado placeholder)
  - index.html: controles de rotación (0-360°), flip (2 checkboxes),
    opacidad (0-100%), borde (check + color + grosor),
    sombra (check + offset xy + color), color placeholder (show/hide por pack)
  - script.js: listeners + initToggle para borde/sombra overlay,
    show/hide color picker según pack seleccionado
  - Tests: 5 tests nuevos (rotación, flip_x, opacidad, borde, sombra)

DÍA 11c (v1.3): Modo "collage" — N imágenes con variación
  - LayoutEngine.calcular_colocaciones("collage", params):
    - Genera N colocaciones con posiciones aleatorias
    - Parámetros: cantidad, tam_min, tam_max, separacion/superposicion,
      rotacion_aleatoria, area (izq/der/arriba/abajo/alrededor)
  - index.html: sliders para cantidad, rango de tamaño, toggle
    separación/superposición
  - script.js: preview con todas las imágenes del collage
  - Tests: collage con N colocaciones en rango de tamaño

DÍA 11d (v1.3): Modo "tapiz" — patrón repetitivo
  - LayoutEngine.calcular_colocaciones("tapiz", params):
    - Cuadrícula N×M con una o más imágenes
    - Parámetros: direccion (horizontal/vertical/diagonal),
      tam_celda, espaciado, rotacion
  - index.html: selector de dirección, tamaño de celda, espaciado
  - script.js: preview con patrón visible
  - Tests: tapiz con cuadrícula correcta

DÍA 11e (v1.3): Tests finales + refinamiento + packs heritage
  - tests/test_layouts.py completo (todos los modos)
  - tests/test_base.py: rotación, flip, opacidad, borde, sombra
  - tests/test_carousel.py: carrusel con overlay genera slides
  - Descargar e instalar un pack de HeritageType como contenido real
  - Refinar detalles visuales en CSS
  - Probar flujo completo: form → preview → descarga ZIP con overlays

v1.4 (Futuro): Editor visual con canvas en vivo
  - Gestión de capas completa
  - Arrastrar, soltar, redimensionar en navegador
  - Preview en tiempo real


MVP v1.0 ✅ COMPLETADO
-----------------
1. Abrís http://localhost:5000
2. Ves un formulario con textarea + selector de paletas + selector de fuente + slider de tamaño + alineación
3. Escribís un texto, elegís opciones, apretás "Generar"
4. Se descarga un ZIP con los slides generados
5. Elegís carpeta en el diálogo Guardar aso

MVP v1.1 (Días 7-9 ✅)
---------------------------
1. Abrís http://localhost:5000
2. Ves split layout: formulario a la izquierda, preview a la derecha
3. Formulario organizado en secciones (Texto, Tipografía, Color, Borde, Sombra, Slide) con grids compactos
4. Seleccionás opciones, el preview se actualiza automáticamente (AJAX) en la columna derecha
5. Navegás entre slides con los botones ◀ ▶
6. Apretás "Generar Carrusel" (botón en columna derecha)
7. Se descarga un ZIP con los slides — elegís carpeta en Guardar como


PRUEBAS UNITARIAS
-----------------
- Framework: pytest (pip install pytest)
- Cada módulo tiene su archivo de test en tests/
- Se corren con: pytest tests/ -v
- Tests se escriben DESPUÉS del código de cada módulo
- Ejemplo:
  Día 2: creás colors.py → escribís test_colors.py
  Día 3: creás carousel.py → escribís test_carousel.py


REGLAS DE TRABAJO
-----------------
- Todo el código lo escribís VOS desde tu teclado.
- Yo soy guía: explico, sugiero, corrijo errores.
- Cada día termina con algo funcionando y visible.
- Primero funciona, después se ve bonito.
